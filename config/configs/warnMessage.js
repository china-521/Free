const extendConflict = `Extended attributes are the same as attribute names in the target object.
 To avoid unpredictable errors, conflicting attributes will be prohibited from being mounted on the target object`;


export default {
	extendConflict,
}